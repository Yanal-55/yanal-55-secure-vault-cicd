name: Secure Vault CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Show Docker version
        run: docker --version

      - name: Show Docker Compose version
        run: docker compose version

      - name: Create test environment
        run: |
          cat > .env <<'EOF'
          POSTGRES_DB=secureapp
          POSTGRES_USER=secureuser
          POSTGRES_PASSWORD=SecurePassword123
          DB_NAME=secureapp
          DB_USER=secureuser
          DB_PASSWORD=SecurePassword123
          DB_HOST=database
          DB_PORT=5432
          EOF

      - name: Check project files
        run: |
          ls -la
          ls -la backend
          ls -la frontend

      - name: Build application
        run: docker compose -p secure-vault-ci-${{ github.run_id }} build

      - name: Start application
        run: docker compose -p secure-vault-ci-${{ github.run_id }} up -d

      - name: Check containers
        run: docker compose -p secure-vault-ci-${{ github.run_id }} ps

      - name: Test backend
        run: |
          sleep 10
          curl -f http://localhost:5000/

      - name: Stop application
        if: always()
        run: docker compose -p secure-vault-ci-${{ github.run_id }} down -v --remove-orphans
